# Required Dependencies for Date Picker

Please run the following command to install the required dependencies for the shadcn date picker:

```bash
npm install date-fns react-day-picker
```

Or if you use yarn:

```bash
yarn add date-fns react-day-picker
```

After installation, the date picker components will work properly.
